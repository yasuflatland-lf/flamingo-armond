# backend/cmd/schema-lint/allowlist.txt
#
# Mutations frozen as bare-type returns at issue #163's introduction.
# Format: one mutation name per line, optional inline "#" comment with the
# reason promotion is deferred. Blank lines and lines starting with "#" are
# ignored.
#
# To promote a mutation: delete the line, change schema + usecase + resolver +
# frontend in the same PR, then run `go run ./cmd/schema-lint` to verify.
# See docs/backend/error-wrapping/outcome-union-enforcement.md for the full
# 7-step promotion checklist.

importCards            # uses ImportCardsPayload — different shape
adminImportMasterCards # uses ImportCardsPayload — mirrors importCards (admin-gated)

# The only typed variant SeedDefaultStarters emits is ucerr.ErrUnauthenticated,
# which is a protocol-level signal (the caller must sign out), not a business
# outcome a client would switch on — a union variant adds no schema structure
# here. The mutation could already return UNAUTHENTICATED via requireCallerSub;
# that emission was invisible to the body-only walker because it is
# helper-delegated (see docs/backend/error-wrapping/outcome-union-enforcement.md
# § "Typed-error emission detection"). Making the deleted-owner branch explicit
# only surfaced the pre-existing condition.
seedDefaultStarterCardgroups # emits only ucerr.ErrUnauthenticated — protocol signal, not a union variant
